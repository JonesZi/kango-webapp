<h1>This is screen one</h1>
<p>I am being loaded by default aka update:false</p>
<button id="goScreenTwo">Go to screen two</button>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const btn = document.getElementById('goScreenTwo');

        btn.addEventListener('click', () => {
            // Make an AJAX request to update the status on the server
            fetch('/update-status', { method: 'POST' })
            .then(response => response.json())
            .then(data => {
                // Handle the response from the server, if needed
                console.log("bam");
            })
            .catch(error => {
                // Handle any errors that occurred during the request
                console.error('An error occurred:', error);
            });
        });
    });

</script>